# build_install.sh 가 읽는 카메라 접속 정보.
#
#   cp camera.env.example camera.env
#   vi camera.env          # 실제 비밀번호를 넣는다
#
# camera.env 는 .gitignore 에 있다. 이 example 파일만 커밋된다.
# 비밀번호를 이 파일(.example)에 적지 말 것 — 이건 커밋된다.

CAMERA_IP=192.168.0.13
CAMERA_USER=admin
CAMERA_PASS=
