# 카메라 자격증명 — 이 파일을 camera.env 로 복사한 뒤 실제 값을 채우세요.
#   copy camera.env.example camera.env
#
# camera.env 는 .gitignore 대상이라 커밋되지 않습니다.
# 환경변수 RP_CAM_USER / RP_CAM_PASS 가 있으면 그쪽이 우선합니다.
RP_CAM_USER=admin
RP_CAM_PASS=여기에_카메라_비밀번호
